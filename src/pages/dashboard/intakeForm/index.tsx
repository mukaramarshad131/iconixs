import GeneralTab from './general-tab';

function IntakeForm() {
  // const theme = useThemeToken();
  return (
    <div className="p-2">
      <GeneralTab />
    </div>
  );
}

export default IntakeForm;
